import 'package:flutter/material.dart';

class VenueCard extends StatelessWidget {
  const VenueCard({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement venue card widget
    return const SizedBox.shrink();
  }
}
